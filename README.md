
  # auto concierge

  This is a code bundle for auto concierge. The original project is available at https://www.figma.com/design/6c2dkzMEKmGF4cWTAECb5A/auto-concierge.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  